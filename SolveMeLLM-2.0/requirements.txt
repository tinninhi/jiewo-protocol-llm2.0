# SolveMeLLM-2.0 依赖包
# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 自然语言处理
transformers>=4.30.0
tokenizers>=0.13.0
datasets>=2.12.0

# 加速和优化
accelerate>=0.20.0
bitsandbytes>=0.41.0
apex>=0.1.0

# 科学计算
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# 可视化和监控
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.13.0
wandb>=0.15.0

# 进度条和工具
tqdm>=4.65.0
psutil>=5.9.0

# 数据处理
json5>=0.9.0
pyyaml>=6.0
h5py>=3.8.0

# 测试和验证
pytest>=7.3.0
pytest-cov>=4.1.0

# 开发工具
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0

# 可选依赖（用于高级功能）
# 多模态支持
pillow>=9.5.0
opencv-python>=4.8.0

# Web界面（可选）
streamlit>=1.25.0
gradio>=3.35.0

# 分布式训练（可选）
deepspeed>=0.9.0
fairscale>=0.4.0

# 性能分析（可选）
py-spy>=0.3.0
memory-profiler>=0.60.0 